# =============================================================
# Arabic PDF OCR & Translation Plugin — Dependencies
# =============================================================

# --- Core (required) ---
PyMuPDF>=1.23.0             # PDF rendering to images
Pillow>=10.0                # Image handling and fallback preprocessing
numpy>=1.24                 # Array operations for image processing

# --- OCR engines (install at least one) ---
pytesseract>=0.3.10         # Tesseract OCR wrapper
easyocr>=1.7                # EasyOCR (deep learning OCR)
# paddleocr>=2.7             # PaddleOCR (optional, uncomment if needed)
# paddlepaddle>=2.5          # PaddlePaddle backend for PaddleOCR

# --- Image preprocessing ---
opencv-python-headless>=4.8 # OpenCV for image preprocessing (headless for servers)

# --- Translation APIs (install based on which methods you use) ---
anthropic>=0.39.0           # Anthropic Claude API
openai>=1.40                # OpenAI GPT API
deepl>=1.16                 # DeepL official Python library
httpx>=0.25                 # HTTP client (used by Google Translate fallback)
# google-cloud-translate>=3.12  # Google Cloud Translation (optional, needs GCP setup)

# --- Utilities ---
requests>=2.31              # HTTP fallback
